import styled from '@emotion/styled';

import {
    space,
    layout,
    color,
    typography,
    flexbox,
    grid,
    background,
    border,
    position,
    shadow,
} from 'styled-system';

export const Box = styled('div')(
    color,
    space,
    layout,
    typography,
    flexbox,
    grid,
    background,
    border,
    position,
    shadow,
);